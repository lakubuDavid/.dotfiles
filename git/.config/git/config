[http]
	postBuffer = 524288000
	version = HTTP/1.1
	maxRequestBuffer = 524288000
[user]
	email = lakubudavid@gmail.com
	name = lakubuDavid
	signingkey = BE9B241F43F7085A
[commit]
	gpgSign = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[core]
    pager = delta
	compression = 9
	autocrlf = input
	whitespace = error
	preloadindex = true
[init]
	defaultBranch = dev

[status]
	branch = true
	showStash = true
	showUntrackedFiles = all

[diff]
	context = 3
	renames = copies
	interHunkContext = 10

[color "diff"]
	meta = white bold
	frag = magenta
	context = white
	whitespace = yellow reverse
	old = red
[color "decorate"]
	HEAD = green
	branch = blue
	tag = yellow
	remoteBranch = magenta

[log]
	abbrevCommitt = true
	graphColors = blue,yellow,cyan,magenta,green,red
	
[push]
	autoRemote = true
	default = current
	followTags = true

[pull]
	default = true
	rebase = true

[rebase]
	autoStash = true
	missingCommitsCheck = warn

[pager]
	diff = delta --diff-so-fancy
	blame = delta

[url "https://github.com/lakubudavid/"]
	insteadOf = ld:
[url "https://github.com/Solution-Inc/"]
	insteadOf = solution:
[url "https://github.com/"]
	insteadOf = gh:

[delta]
	line-numbers = true
	side-by-side = true
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection

[interactive]
    diffFilter = delta --color-only

[merge]
    conflictstyle = zdiff3    
